{
  "name": "record-bin-lambda-function",
  "version": "1.0.0",
  "description": "A Serverless functions that receives a webhook from a record deletion and handles it to create a backup version on a bin",
  "main": "index.js",
  "scripts": {
    "test": "node --test --import tsx \"tests/**/*.test.ts\""
  },
  "keywords": [
    "datocms"
  ],
  "author": "Marcelo Finamor Vieira",
  "license": "MIT",
  "dependencies": {
    "@datocms/cma-client-node": "^5.3.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsx": "^4.20.6"
  }
}
