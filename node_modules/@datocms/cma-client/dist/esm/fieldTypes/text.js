import { isLocalizedFieldValue, } from '../utilities/normalizedFieldValues';
export function isTextFieldValue(value) {
    return typeof value === 'string' || value === null;
}
export function isLocalizedTextFieldValue(value) {
    return (isLocalizedFieldValue(value) && Object.values(value).every(isTextFieldValue));
}
//# sourceMappingURL=text.js.map