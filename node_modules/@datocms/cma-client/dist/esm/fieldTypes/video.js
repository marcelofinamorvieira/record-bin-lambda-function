import { isLocalizedFieldValue, } from '../utilities/normalizedFieldValues';
export function isVideoFieldValue(value) {
    if (value === null)
        return true;
    return (typeof value === 'object' &&
        value !== null &&
        'provider' in value &&
        'provider_uid' in value &&
        'url' in value);
}
export function isLocalizedVideoFieldValue(value) {
    return (isLocalizedFieldValue(value) &&
        Object.values(value).every(isVideoFieldValue));
}
//# sourceMappingURL=video.js.map