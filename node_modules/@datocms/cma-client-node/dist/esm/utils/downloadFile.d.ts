import { CancelablePromise } from '@datocms/rest-client-utils';
import { OnProgressInfo } from './uploadLocalFileAndReturnPath';
declare type Options = {
    onProgress?: (info: OnProgressInfo) => void;
};
export declare type DownloadResult = {
    filePath: string;
    deleteFile: () => Promise<void>;
};
export declare function downloadFile(url: string, { onProgress }?: Options): CancelablePromise<DownloadResult>;
export {};
