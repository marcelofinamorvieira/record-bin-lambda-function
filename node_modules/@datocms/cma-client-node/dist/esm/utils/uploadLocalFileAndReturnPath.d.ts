import { Client } from '@datocms/cma-client';
import { CancelablePromise } from '@datocms/rest-client-utils';
export declare type OnProgressDownloadingFileInfo = {
    type: 'DOWNLOADING_FILE';
    payload: {
        url: string;
        progress: number;
    };
};
export declare type OnProgressUploadingFileInfo = {
    type: 'UPLOADING_FILE';
    payload: {
        progress: number;
    };
};
export declare type OnProgressRequestingUploadUrlInfo = {
    type: 'REQUESTING_UPLOAD_URL';
    payload: {
        filename: string;
    };
};
export declare type OnProgressInfo = OnProgressRequestingUploadUrlInfo | OnProgressDownloadingFileInfo | OnProgressUploadingFileInfo;
export declare type Options = {
    filename?: string;
    onProgress?: (info: OnProgressInfo) => void;
};
export declare function uploadLocalFileAndReturnPath(client: Client, localPath: string, options?: Options): CancelablePromise<string>;
